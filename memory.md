# MEMORY.md — Read This First in Any New Session

**Purpose:** Ye file is poore project ki conversational memory hai. Agar ye ek naya laptop hai ya naya chat session hai jisme pehle ki baatcheet yaad nahi hai — **ye file sabse pehle padho**, phir `tracker.md` (live progress log). Dono milkar poora context de dete hain jaisa purane chat me tha.

---

## 1. User kaun hai, kaise kaam karna hai

- User Hinglish me baat karta hai — main bhi Hinglish me hi respond karta hoon (simple, clear, detailed).
- **Collaboration protocol (strict):**
  1. Koi bhi development step shuru karne se pehle, us step me kya banega — simple bhasha me, poora detail ke saath — samjhaana hai, aur user ka explicit confirmation aane ke baad hi code likhna hai.
  2. Har step complete hone ke baad `tracker.md` turant update karna hai (Ongoing → Completed, agla item Pending se Ongoing me).
  3. Phase order strictly follow karna hai (Phase 1 → 2 → 3 → 4 → 5), DoD gate green hue bina agla phase start nahi karna. **Isse explicitly, actively verify karna hai** (tracker.md ka DoD checklist khol ke item-by-item check karna, real test chala ke) — sirf "maine pehle bola tha ye done hai" wale narrative pe trust nahi karna. 2026-08-13 ko exactly ye gap hua tha: Phase 3 ka DoD checklist tracker.md me `- [ ]` (unchecked) pada tha kaafi der se, sab kaam ho chuka tha lekin explicitly verify/mark nahi kiya gaya — user ne khud pucha "phase 3 gate pass he ya nahi" tabhi pata chala ki 2 items (unsubscribe endpoint ka real HTTP test, QC ka real-LLM-se bad-draft-reject test) sirf mocked the, real test kabhi nahi hua tha. Dono turant real test likh ke close kiye. **Lesson:** Phase-transition se pehle, khud checklist verify karo — user ko bar-bar pucchna na pade.
- **User khud backend/server run aur test karna pasand karta hai** — jab live server chalana ho (jaise `python app.py` chalana, browser me hit karna), to sirf clear steps de do, khud mat chalao. Lekin one-off automated checks (test client se ek route hit karna, `migrate.py` chalana, pytest) khud kar sakta hoon — ye "live app chalane" wali restriction me nahi aata.
- Koi bhi risky/destructive action (delete, force push, gitignore banana) se pehle confirm karna — user ne khud kai baar explicitly bola hai "abhi mat karo, baad me bataunga" jaisi cheezein, unko respect karna.
- **Project ab real VPS pe live hai (`sales.ivinfotech.com`, 2026-08-18 se) aur mere paas uska real SSH/root access hai.** Credentials `backend/.env` me `VPS_HOST`/`VPS_SSH_USER`/`VPS_SSH_PASSWORD`/`VPS_APP_PATH`/`VPS_WEBROOT` naam se hai — wahi se padhna, kisi bhi git-tracked file (yehi memory.md ya tracker.md) me actual password kabhi nahi likhna (dono GitHub pe push hoti hai, `.env` hi safe hai). Connect karne ka tareeka: is dev machine pe `sshpass`/`plink` nahi hai, `paramiko` use karo (`py -3.11 -m pip install --user paramiko`, project ke apne `backend/venv` se alag). Full architecture aur ab tak ke incidents tracker.md ke **A.4** rule aur "⭐ VPS PRODUCTION DEPLOYMENT" entry me hai — koi bhi VPS kaam se pehle wo padhna.

## 2. Project kya hai

Project ka naam **AI-BOS (Enterprise AI Business Operating System)** hai — pehle ye "Autonomous AI Sales Operating System" tha, 2026-08-10 ko upgrade kiya gaya ek naye **Enterprise Executive Layer (Chapter 15 / §8)** ke saath jo Cognitive Brain Layer ke upar baithta hai.

**Real-world purpose (confirmed 2026-08-12):** Ye user ki apni company **IVinfotech** ke liye production use hoga — dono, unka SaaS product AUR unki custom-dev IT services (Dual Sales Mode Engine, Phase 5, already isko support karta hai — koi alag multi-tenant "sabke liye SaaS" scope abhi nahi chahiye, deliberately deferred). **Testing/dev ke dauran koi bhi real third-party business ko outreach nahi hoga** — live-send test IVinfotech ke apne contact info pe hoga jab Step 3.3/3.4 (actual email/WhatsApp sending) ban jayenge.

**Ab code-level enforcement bhi hai (2026-08-13):** jab discovery scheduler (Step 3.5) real products ke against live chalaya gaya, real businesses discover/score hone lage — aur scheduler ka hourly outreach-tick unhe khud-b-khud real email/WhatsApp bhej sakta tha bina kisi confirmation ke (1 real WARM lead already eligible tha jab pakda gaya, user ne khud "kisiko outreach na ho jaye" bol kar alert kiya). Fix: `Config.AUTONOMOUS_OUTREACH_ENABLED` (default `false` in `.env`) — jab tak explicitly `true` na ho, scheduler ka outreach tick kuch bhi nahi karta, discovery/scoring free chalta rehta hai. **Ye tab tak `false` rakhna hai jab tak user explicitly na bole "ab real businesses ko bhejna hai".**

**3-layer architecture:** Executive Layer (governs — budget/CAC ceilings, capacity throttles, sales-mode routing) → Cognitive Brain Layer (decides — AI agents, Decision Engine, QC veto) → Execution Infrastructure (acts — Flask/SQLite/Playwright/n8n).

### Authoritative docs (sab kuch inhi do me hai — baaki files delete ho chuki hain)
- **`MASTER_DEVELOPMENT_PRD.md`** — single build spec. Phases 1–5, poora DDL (16 tables), saare agent/cognition code blueprints. **Isi ke against build karna hai.**
- **`AI_Sales_Intelligence_PRD_v2.md`** — cognitive/organizational reference (agent roles, decision engine, memory tiers, Chapter 15 ke 8 executive modules).
- **`tracker.md`** — meri apni live progress log. 5 sections: Rules & Memory, Completed, Ongoing, Pending (Phases 1–5), aur **Section 5: Add-on Phases 6–10** (2026-08-19 se). **Har naye session me sabse pehle ye padhna hai** current status jaanne ke liye.
- **`CRM_UI_UX_PLAN.md`** — dashboard→CRM upgrade plan, apne UI phases ke saath (1–4 original, 5–9 add-on jo backend Phase 6–10 ke saath 1:1 paired hain).
- **`NEW_REQUIREMENTS_STAGING.md`** — user ke naye raw requirements ka capture buffer. Naya requirement pehle yahan `RAW` me jaata hai; user confirm kare tabhi teeno PRD docs me merge hota hai (status `MERGED`). Ye workflow user ne khud maanga tha (2026-08-19) — turant PRD edit mat karo.

### Removed docs (ab exist nahi karti, dobara mat banana)
`prd.md`, `ENTERPRISE_BUSINESS_LAYER_ADDON.md`, aur original standalone "PRD v3" file — inka saara content upar wali 2 files me merge ho chuka hai (2026-08-10).

## 3. Git / GitHub setup

- Remote: `https://github.com/ivalema35/AIsales_agents_team.git`, branch `main`.
- **`.gitignore`** covers `.venv/`, `venv/` (actual folder name on this machine, no dot — fixed a real gap here 2026-08-11), `.env` (real secrets, added when real `.env` was created with real API keys), `__pycache__/`, `*.pyc`.
  - `sales_system.db`/`-shm`/`-wal` **ab git se untrack hai (2026-08-19, reverses the 2026-08-11 decision)** — VPS live hone ke baad local dev DB aur VPS ki real production DB genuinely alag data rakhte hain, isliye DB ko git me rakhna risky ho gaya (ek `git pull` VPS ki real leads ko overwrite kar sakta tha). File disk pe as-is hai, bas `git rm --cached` + `.gitignore` me add kiya. Poora incident/recovery detail tracker.md me hai.
- User agla laptop change karne wala hai — isliye jitna zyada context repo (git) me ho utna better, kyunki mera internal Claude memory system is machine tak local hai, naye laptop pe transfer nahi hoga.
- **Deployment workflow rule (2026-08-19):** major change/naya module complete hone ke baad hi (chhote tweaks ke liye nahi) — user se confirm karna, phir: `git push` (local→GitHub) → VPS par `git pull` (DB files ko touch kiye bina, tracker.md §A.5/§A.4 ka safe pattern follow karke) → frontend change ho to build+`public_html` sync → affected `bos-*` services restart + verify. Poora detail tracker.md §A.5 me.

## 4. Build status (as of 2026-08-13 — full detail always in tracker.md, this is a curated summary not a log)

**Phase 1 (Foundation & Core REST API) — ✅ COMPLETE, DoD gate green.** Flask skeleton, 16-table DB + pragma listener, Product CRUD, Lead CRUD. All 422-not-500 validated, FK cascade confirmed live.

**Phase 2 (Async Scraper + LLM Scoring) — ✅ COMPLETE, DoD Gate P2 green.**
- Step 2.1 — Durable job queue (atomic claim, 10-thread contention tested).
- Step 2.2 — Serper.dev (Places) + Hunter.io providers, real API calls verified.
- Step 2.3 — Async scraper runner + Playwright fallback, zero browser leaks (50× + real + exception paths).
- Step 2.3b/2.3c — Full contact-enrichment waterfall (website scrape → Hunter → Serper snippets → Maps circuit-breaker, for both email and phone), rebuilt from a user-caught wrong-email bug into a 6-item verified checklist. **Real, cross-category results: phone 0/10 → 10/10, email accuracy fixed (was silently wrong on some leads, not just low-coverage).**
- Step 2.4 — First LLM calls in the project (`cognition/llm_client.py`, swappable Gemini/OpenAI; `agents/review_analyst_agent.py`; `agents/scoring_agent.py`). Decision Engine routing reproduces MASTER PRD's own DoD test exactly. Real Gemini calls proved both the positive path (genuine pain-point extraction) and graceful degradation (503/quota-exhaustion → safe defaults, no crash).

**Phase 3 (n8n, Atomic Claiming, Multi-Channel Outreach) — started:**
- Step 3.1 — `services/lead_service.py`, `claim_lead_for_outreach()`. Atomic SCORED→OUTREACHING claim (rowcount-checked, same pattern as job_queue's `claim_next`), then enqueues `OUTREACH_EMAIL`/`OUTREACH_WA` per available channel — both if both exist, never forced to pick one (an early design draft got this wrong; corrected before building). Eligible only if tier HOT/WARM AND scoring confidence wasn't low enough to have routed to `HUMAN_ESCALATION`. **Deliberately not auto-chained from `_handle_score`** — real-sending safety boundary, see §2 above. 10-thread contention test passed (exactly 1 winner, exactly 1 job per channel).
- Step 3.2 — `services/outreach/suppression.py`, `is_suppressed()`/`add_suppression()`. The ONLY module allowed to touch `suppression_list` — every future send path must check this immediately before sending, unconditionally (MASTER's 100% rule). Normalization reuses existing conventions (email lowercase, phone via the same `normalize_mobile()` from Step 2.3c) so a suppression recorded one way is never missed by a differently-formatted check. Idempotent via the table's own UniqueConstraint (catches `IntegrityError`, not a race-prone check-then-insert). 10-concurrent-adds-of-the-same-identifier test passed (zero crashes, exactly 1 row survives).
- **Step 3.3 — Compliant email sending. First real message this system has ever generated AND sent.** `agents/outreach_agent.py` (drafts, never writes its own footer) + `agents/quality_controller_agent.py` (absolute veto, fails closed on its own error) + `services/outreach/email_service.py` (Resend via plain `requests`, appends compliant footer/unsubscribe itself) + `api/unsubscribe.py` (one-click GET route) + `jobs/outreach_handler.py` (registered into `jobs/worker.py` — deliberately NOT `async_runner.py`, since outreach needs sequential pacing, not concurrent fan-out; this is `worker.py`'s first real handler since Step 2.1 built it). Flow: suppress-check → draft → QC → retry-once-with-feedback if rejected → escalate (not send) if still rejected → **suppress-check again immediately before the actual send** → send → log. **Resend sandbox constraint turned out useful**: no domain verified yet, so sending is structurally restricted to the email the Resend account itself was signed up with (a personal Gmail, not IVinfotech's domain) — this enforces "test on ourselves only" for free, no extra code needed. 22 mocked checks + **one real live send**, confirmed delivered: real Gemini draft → real QC approval (0.95 confidence) → real Resend send → landed in the user's own inbox.
- **Reminder before any REAL lead is ever emailed:** set `COMPANY_PHYSICAL_ADDRESS` in `.env` (currently a placeholder) to IVinfotech's real registered address, and verify IVinfotech's actual domain in Resend.
- **Step 3.4 — WhatsApp Cloud API. Real send confirmed by the user.** User doesn't have direct Meta access, only credits with a 3rd-party BSP (`waba.fortius.in.net`). **Verified legitimacy before integrating** (MASTER's own rule requires official-API-only, to rule out ToS-violating QR-code/personal-WhatsApp automation): no personal-device linking involved, dashboard says "Official Meta Partner", and — the conclusive check — the send endpoint's path (`/{version}/{phoneNumberId}/messages`) is a character-for-character match to Meta's real Cloud API structure. Discovered real `phoneNumberId`/`wabaBusinessId` live via `GET /{version}/channels`. Of IVinfotech's 18 pre-existing approved templates, only one (`marketing_gen`) was usable for outbound prospecting — the rest are internal-ops templates (career apps, admin alerts). **User's idea ("AI should create templates via the API") was refined, not rejected**: Meta approval isn't instant and rapid template creation looks spammy, so per-lead template generation is structurally impossible — landed on a small curated `TEMPLATE_LIBRARY` (`services/outreach/whatsapp_templates.py`) that the system selects from per-lead (fast), with **library growth** (new template proposals) as a periodic/asynchronous process — exactly where "AI creates templates" fits. Demonstrated this live today: drafted and submitted a proper 2-variable pain-point template via `POST .../message_templates`, got back `status: PENDING` (Meta review in progress). Built `whatsapp_service.py` (send), `whatsapp_templates.py` (library/selection/fill/validate -- no LLM needed, template vars are plain substitution not generation), `jobs/outreach_wa_handler.py` (registered in `jobs/worker.py`, same pacing reasoning as email). 21 mocked checks + real send via `marketing_gen`, confirmed delivered by the user on WhatsApp.
- **Follow-up done (2026-08-13):** `ivinfotech_pain_point_outreach` confirmed `APPROVED` by Meta same day. Activated in `TEMPLATE_LIBRARY`; since pain-point codes are freely LLM-invented (no fixed list) and this template's wording is category-agnostic, `select_template()` now uses it for any lead with a known pain point rather than requiring a `PAIN_POINT_CATEGORY_MAP` code match — that map stays reserved for a future genuinely category-specific template.
- **Step 3.5 — Autonomous Discovery Scheduler (2026-08-13), replaces n8n entirely.** User pushed back on the original n8n-based plan twice: (1) no Docker on the dev machine, and their real n8n (self-hosted at `ai.ivinfotech.com`) can't reach a local-only Flask backend without a public URL — so n8n's only real job here (cron scheduling) got replaced with a small dedicated always-on Python process, `jobs/discovery_scheduler.py`. (2) User explicitly did not want to manually type city/keywords daily — wanted the AI to decide who to target per product on its own, visibly, with the option to add more manually. This revived MASTER PRD §6's `ICP_STRATEGY_AGENT_SYSTEM_PROMPT`, which was designed in the original PRD but never actually wired into any step until now — `agents/icp_strategy_agent.py`. New `products.target_regions` (human sets once, multiple regions supported) is the only manual input left; AI decides business-type/keywords/target-complaints within that boundary. New `product_strategies` table stores AI + human-added search queries (versioned, human additions never overwritten by AI refresh). Same process also closed the previously-open "pacing caps" DoD item (per-channel daily send caps + staggered `run_after`). **Proven with a real end-to-end test**: product registered with only a description + `target_regions=["Ahmedabad"]`, zero manual keywords — system decided search queries itself, fired a real discovery search on its own, and produced 9 real leads with no further human input. Full design rationale in tracker.md **A.2**.
- **User explicitly flagged as a "next level" priority (2026-08-13, not built yet, deliberately deferred):** a fully autonomous WhatsApp template lifecycle — system itself decides when a new template is needed, drafts it via LLM, submits via Create Template API, and auto-detects Meta approval to activate it (no manual code edit). Today all of this is proven possible but done BY HAND (I called the APIs directly, not the running system). My recommendation, which the user accepted: don't build this until real campaign performance data exists (`campaign_variants` not wired up yet, zero real-lead sends so far) — otherwise it's guesswork, not a data-driven Learning Agent. Full rationale in tracker.md under "⭐ USER-FLAGGED IMPORTANT". **Revisit this when real campaign history exists**, and remind the user this is tracked if the topic of "smarter WhatsApp automation" comes up again.

**Phase 3 → Phase 4 transition (2026-08-13), real production setup + first live run:**
- Real setup completed: real IVinfotech address in email footers, `ivinfotech.com` verified in Resend, sender identity settled on `ronakpatel@ivinfotech.com` after 2 rejected attempts (own name, then an owner's real existing mailbox — see the auto-memory system's `feedback_no_personal_name_as_sender`), 7 real products registered (IV Classes + 6 IT services), each its own row with Gujarat `target_regions`.
- **MAJOR real bug, ICP strategy agent (2026-08-18):** user spotted bad-quality discovery leads for Website Development + AI Automation and asked to investigate. Root cause: `ICP_STRATEGY_AGENT_SYSTEM_PROMPT` let the LLM propose search queries describing the PRODUCT's own service ("website design", "customer support") instead of only the PROSPECT's business vertical — a Places search on those terms returns other companies that PROVIDE that service (competitors), not businesses that need it. Confirmed with real data: Website Dev's whole 59-lead batch was functionally 100% IT/web/software agencies; AI Automation's 98-lead batch was ~45% telecom/appliance "customer care service centers" + lead-gen/CRM agencies. Same bug found (unprompted, while investigating) in a third, older product, Mobile App Development (`is_active=0`, so silently not even getting fresh discovery) — with a real consequence: the flawed query matched IVinfotech's own listing, creating a lead for the company's own contact (`disha@ivinfotech.com`) that genuinely received 5 real outreach sends. Fixed: prompt now has an explicit rule + negative examples forbidding product/service-name queries; all 3 products' strategies force-regenerated (verified via real LLM call); 157+60 contaminated leads soft-rejected (status flip + `AgentEvent`, nothing deleted; the long-running real test lead "GameZone Visnagar," also cross-linked to Mobile App Dev, deliberately left untouched). Re-verified live with a real small-scale discovery run through the actual pipeline (also found `scraper_worker.async_runner` — the process that actually handles DISCOVER/ENRICH/REVIEW/SCORE — wasn't running this session despite everything else being up; started it): all 60 new leads across the 3 products were correctly-targeted real small businesses, zero self-referential matches. User confirmed this was a one-off verification run, not a request to leave `discovery_enabled` on.
- **Big CRM UI/UX iteration round + several more real bugs (2026-08-17/18, full detail in tracker.md).** Long screenshot-driven session, each round following explain→confirm→build→verify: trend chart redesigned (candle bars, Weekly/Monthly only); by-product view rebuilt as per-product tier donuts (`ProductTierDonuts.jsx`, small multiples — one big multi-product pie would have been the dataviz skill's own anti-pattern); **found and fixed a real alerts-panel visibility bug** (a lead that had JUST auto-escalated from a genuine interested reply was hidden by the same exclusion rule meant for the OLD "claim" flow — the single most urgent thing on the dashboard was invisible); **found there was no way anywhere in the UI to close the loop** on a lead (mark Converted/Lost) even though the backend already supported it — added; **found a suppression-visibility gap** (an opted-out lead's `status` never changed and nothing showed it, so a human could still try to send to them) — added `is_suppressed` everywhere a lead renders, hides Send. Also: new full `/leads` page (search/filters/pagination/tier-colored rows), LeadDetail Prev/Next navigation (surfaced a real SQLite datetime-comparison bug — SQLAlchemy's `.000000`-suffixed bound param vs a bare `CURRENT_TIMESTAMP` value made a row compare as less-than-itself, fixed with `func.strftime()` normalization, found via `echo=True` logging not guessing), Kanban rejected twice before an explicit `AskUserQuestion` settled it on columns+per-column-pagination, native `window.confirm()` replaced app-wide with a real modal, Products page got real in-place editing (previously impossible after creation).
- **Major build: real "Seen" tracking for WhatsApp + Email, from scratch (2026-08-18).** Neither channel had ANY real "message was actually seen" signal before — user asked if a Sent/Seen/Reply chart was feasible, got an honest "not yet, that data doesn't exist," then the real infrastructure got built rather than the metric being faked. WhatsApp: Meta already sends `read` receipts via a `statuses` array on the same webhook that was previously 100% ignored — now captured. Email: brand-new Resend webhook (`api/webhooks.py`) for `email.opened`/`email.clicked`, needed the user to set up their own Resend-dashboard webhook + open-tracking domain (they did, guided through it). New `OutreachLog.provider_message_id`/`read_at` columns capture each send's provider ID so a later read-event can be matched back to it. **Verified with a genuine real send → real open in a real mail client → real `read_at` landing on the right row**, not a simulated payload.
- **Project ab REAL VPS pe LIVE hai — `sales.ivinfotech.com` (2026-08-18/19).** User ne khud VPS pe deploy kiya (AlmaLinux 9.8, CyberPanel), 5 systemd services (`bos-api`/`bos-worker`/`bos-scraper`/`bos-poller`/`bos-scheduler`) sab `/home/sales.ivinfotech.com/aisales/backend` se chal rahe hai, sab `enabled+active`. **VPS SSH credentials `backend/.env` me `VPS_HOST`/`VPS_SSH_USER`/`VPS_SSH_PASSWORD`/`VPS_APP_PATH`/`VPS_WEBROOT` ke naam se hai** — yahan ya tracker.md me jaan-bujh kar NAHI likhe, kyunki ye dono files GitHub pe push hoti hai aur project ka apna hi non-negotiable rule hai "secrets sirf .env me" ([[reference_n8n_hosting]] jaisa hi pattern — .env safe hai, .md files nahi). Is session me `sshpass`/`plink` nahi tha, to `paramiko` (Python SSH library) use kiya, project ke apne venv me nahi (alag se `py -3.11 -m pip install --user`) — future me VPS access ke liye yehi tareeka reuse karo.
  - **User ne 2 cheezein poochi: (1) Python 3.9 compatibility changes check karo, (2) "WhatsApp msg nahi ja rahe" debug karo.** Dono resolve ho gaye:
  - **(1) Compat fix sahi thi lekin 2 real bugs mile, dono fix kiye**: 47 files me `from __future__ import annotations` (3.9 pe `str | None` jaisi syntax ke liye zaroori) daala gaya tha, lekin docstring SE PEHLE — jisse har module ka `__doc__` silently `None` ho gaya tha (verify kiya: `api.analytics.__doc__` genuinely `None` tha future-import docstring se pehle hone ki wajah se). 15 files me future-import 2-3 baar duplicate bhi ho gaya tha. Dono fix kiye (docstring pehle, future-import baad me, dedupe) — sab files `py_compile` se clean, `app.create_app()` abhi bhi kaam karta hai.
  - **(2) "WhatsApp msg nahi ja rahe" ka asli root cause: WhatsApp/backend ka bug tha hi nahi — frontend hi kabhi deploy nahi hua tha.** Backend 100% sahi tha (`bos-worker`'s journalctl log me real WhatsApp sends dikhe, `sales_system.db` me 8 real SENT rows, zero FAILED jobs). Lekin `public_html/` (jo actual web-serve hota hai) me sirf CyberPanel ka default "Installed" placeholder page tha — real React build (`frontend/dist/`) kabhi copy hi nahi hua tha wahan. User jo bhi test kar raha tha, wo real app hi nahi tha, koi button kaam nahi karega. Fixed: placeholder backup kiya (`/root/backup_public_html_placeholder/`), `dist/*` ko `public_html/` me copy kiya, ownership match ki. Verified: `https://sales.ivinfotech.com/` ab real `<title>AI-BOS</title>` app serve karta hai. **Yaad rakhna**: `frontend/dist/` (build) aur `public_html/` (serve) do ALAG jagah hai — future me har frontend change ke baad manually re-copy + re-chown karna padega, koi deploy script abhi automate nahi karta ye step.
  - **Ek real fragility note**: `.env`'s `DB_PATH=sales_system.db` relative path hai — sirf isliye kaam karta hai kyunki har systemd service ka `WorkingDirectory` sahi set hai. Agar koi naya service bina `WorkingDirectory` ke add hua to wo silently ek alag empty phantom DB bana lega (SQLite auto-creates) — jobs zero-trace vanish ho jayenge. Khud isi mistake me phasa (apna hi diagnostic script `/root` cwd se chala, empty `/root/sales_system.db` ban gaya) — caught aur delete kiya.
  - **Dependency version skew flag kiya (verified working via real logs, but not exhaustively tested)**: `requirements.txt` me exact versions pin nahi hai, VPS (Python 3.9) purane majors resolve karta hai (`openai` 2.48.0 vs dev ka 3.0.0, `google-genai` 1.47.0 vs 2.17.0). `llm_client.py` ka usage check kiya — dono SDK ka sirf stable core API use ho raha hai, aur VPS ke real logs me already dono providers (Gemini + OpenAI fallback) kaam karte hue dikhe.
- **Real security gap band ki — login/session auth banaya, same-day local + VPS dono pe deploy kiya (2026-08-19).** User ne khud point out kiya: VPS pe live hone ke baad, koi bhi URL wale ko poora real lead data + real actions (Send, Mark Converted) mil sakta tha, koi login nahi tha. Single shared admin login banaya (per-user accounts nahi — chhota internal team hai, zaroorat nahi, over-engineering se bacha). **`api/auth.py`** (login/logout/me), Flask session-cookie based (`SECRET_KEY`+hashed password `.env` me), **`app.py`** me global `before_request` gate — har route protected except health/webhooks/unsubscribe (external parties ke liye khule rehna zaroori). Frontend: naya `Login.jsx` + `App.jsx` ab real `GET /auth/me` se decide karta hai login dikhana hai ya app.
  - **User ne explicitly bola "web search karke achha sa lottie animation lao"** — ek "peek-a-boo" character mila (GitHub ke ek reference login-project se) jo typing dekhta hai aur password field pe eyes cover karta hai, khud JSON self-host kiya (external CDN pe depend nahi kiya). LottieFiles.com khud Cloudflare-protected hai (403 diya), lekin uski raw asset CDN subdomains khuli hai — wahan se fetch kiya.
  - **3 real bugs mile, sab REAL BROWSER me render karke pakde, sirf code padh ke nahi**: (1) `lottie-react`'s latest version (v3) ka API hi alag tha (koi default export nahi) — stable v2.4.2 pe pin kiya. (2) Is project ke Vite setup me CJS/ESM interop double-wrap ho raha tha, `import Lottie` actual component ki jagah pura exports object de raha tha — `page.evaluate()` se real browser me confirm karke defensive unwrap likha. (3) `lottieRef.current.setLoop` exist hi nahi karta uss version me — asli method `lottieRef.current.animationItem.setLoop` pe hai. Is environment me koi browser-automation tool nahi hai, to project ke apne `backend/venv` ke `playwright` (already scraper ke liye installed) use kiya real console-error capture ke liye — isi se teeno bug pakde, sirf screenshot se blank page dikhta, wajah pata nahi chalti.
  - **VPS pe bhi turant deploy kiya** (kyunki asli exposure to sirf VPS pe hai, local dev exposed nahi hai) — backend files SFTP se upload kiye, VPS ke apne Python 3.9 venv pe import-check kiya PEHLE (taki live service na toote), phir `.env` me naye keys add kiye, frontend `npm install && npm run build` VPS pe hi chalaya, `public_html/` me copy kiya, `bos-api.service` restart kiya. **Real browser se `https://sales.ivinfotech.com` pe live login flow verify kiya** — dono jagah (local + VPS) alag-alag `SECRET_KEY`/password use kiye (prod/dev secrets kabhi share nahi karte), credentials sirf `.env` me, kahi aur nahi.
  - **Follow-up same din, user ne khud VPS ka screenshot bhejke bataya**: "ankh par hath nahi rakhta, bar bar hath lata he" — real bug tha, "Covering" segment `loop:true` se play ho raha tha, ek hands-up MOTION ko loop karne se hath baar-baar upar jaata (kabhi rukta nahi). Fix: `loop:false` — ek baar play hoke apne last frame pe hi ruk jaata he (hath eyes pe reh jaate hai). **500ms aur 2 second dono pe screenshot leke verify kiya** ki genuinely ruka rehta he, sirf ek frame check karke nahi maana. Sath me UI/UX bhi upgrade kiya — ambient blurred color-orb background, card entrance animation, input icons, gradient button+spinner, error par shake animation. VPS pe dobara deploy + real browser se verify kiya.
  - **Doosra follow-up, same din, ek aur real screenshot ke saath**: user ne bola background logo theme se match nahi karta (purple tha) aur eye-toggle button click karne pe animation "break" ho jaati he. (1) `logo.png` ke actual pixels sample kiye (guess nahi kiya) — real brand colors nikle `#4e535a` (slate) aur `#adb0b4` (gray), pura palette inhi pe rebuild kiya. Sath me 5 flat-icon "chips" add kiye (Target/TrendingUp/Bot/MessageSquare/BarChart3, dashed circuit-lines se connected) mascot ke around — Target khud logo ke bullseye ko echo karta he. (2) Eye-toggle button ka bug reproduce karke confirm kiya (`document.activeElement` genuinely button ban jaata tha click pe) — plain HTML button click pe focus le leta he, jisse password field blur hota tha aur animation turant idle pe girr jaati thi. Fix: `onMouseDown` pe `preventDefault()` — button kabhi focus leta hi nahi, ab click karne pe password field ka focus bilkul disturb nahi hota. Fix se pehle aur baad dono verify kiya. VPS pe dobara deploy kiya, real browser se confirm kiya.
  - **Teesra follow-up, same din, teesra real screenshot**: (1) pichla focus-fix karne se ek naya side-effect aaya — button ab focus nahi churata (sahi), lekin isi wajah se eye-icon click pe koi bhi animation reaction hi nahi ho raha tha ("peak nahi kar raha he"). Fix: button ke apne `onClick` me explicitly `peek` (reveal pe) / `cover` (hide pe) play karwaya, focus/blur event pe depend karne ki jagah — 3 alag poses (focus->cover, reveal->peek, hide->cover-again) screenshot se verify kiye. (2) Pichle round ke flat-icon chips card ke LEFT PANEL ke andar the — user ne clarify kiya ki original ask poore PAGE background ke liye tha, card ke andar nahi ("sirf login wale card par nahi"). Chips + circuit-lines ko card se nikal ke outer page-background wrapper me move kiya, poore viewport pe percentage-based positions se scatter kiya, ek 6th icon (Zap) add kiya balance ke liye. VPS pe dobara deploy + verify kiya.
- **Lead social profiles (Instagram/Facebook/LinkedIn) — user ne 7-step gated plan maanga, har step pe real test pass tabhi agla (2026-08-19).** Pehle poochne pe honestly bataya ki abhi ye data automatically NAHI milta — `serp_provider.py` Instagram/Facebook links detect to karta tha lekin sirf ek trust-signal ke liye, URL khud kabhi save nahi hota tha; website scrape me email/phone ke liye HTML fetch hoti thi but social footer links kabhi nikale hi nahi jaate the; `LeadFirmographics.linkedin_url` column schema me tha lekin poore codebase me kabhi koi row hi nahi banti thi (dead column). Ye sab ek Explore agent se pehle verify kiya, guess nahi kiya.
  - **7 gates, sab PASS, sequentially**: (1) DB me 3 naye columns (`Lead.instagram_url/facebook_url/linkedin_url`) — real DB me confirm kiya. (2) Website ki apni HTML se social links nikalna (`find_social_links`) — constructed test cases se PASS hua, phir real live websites pe test kiya to **ek real bug mila**: tracking query-params (`?igsh=...`) URL me leak ho rahe the aur trailing slash query-string ke baad lag raha tha (malformed) — fix karke dobara verify kiya. (3) Jinki website nahi unke liye Serper search-based lookup (`find_social_profiles`) — real API calls, "GameZone Visnagar" ke liye sahi se `{}` diya (raw results check karke confirm kiya ki genuinely koi real profile nahi tha, bug nahi), "Sparrk" ke liye sahi real handle mila jo isi codebase ke purane comments me already reference tha. (4) Enrichment pipeline me wire kiya — real lead pe chalaya, FRESH DB session se confirm kiya persist hua. (5) Backend API — real GET/PATCH round-trip. (6) Frontend — real Playwright browser se test karte hue **ek aur real bug mila**: save karne ke baad `PATCH` response me `pain_points`/`firmographics` jaise fields nahi aate (sirf GET me aate hai), aur frontend poora `lead` state isi incomplete response se replace kar deta tha — crash. Fix: replace ki jagah merge kiya. (7) VPS pe deploy — sanity-import pehle (live service todne se pehle), migration, frontend build, 3 services restart, production pe real verify.
  - **Lucide-react me koi Instagram/Facebook/LinkedIn brand icon hai hi nahi** (check kiya, generic icon set hai) — naya npm package add karne ki jagah (lottie-react wali dobara dikkat se bachne ke liye) 3 chhote inline SVG icons khud likhe.
  - Sab local + VPS dono jagah verify kiya, tracker.md me poora gate-by-gate detail hai.
  - **Follow-up: user ne khud bola "ek lead pe test karo jiske social URL na ho, dekho milte hai ya nahi"** — "TIME Ahmedabad" pe try kiya, kuch nahi mila. Guess nahi kiya, investigate kiya: website ki apni footer me genuinely kuch nahi tha (sahi), lekin search wale raw results me ek real Instagram AUR ek real LinkedIn profile tha jo humara code miss kar gaya. Wajah nikali: (1) real query me `region_location` (poora messy address) bhi jud jaata he, jo yehi existing pattern he jo email/phone/website search me bhi hai — naya bug nahi, isko yahan fix karna scope se bahar he. (2) **Ek real bug mila jo humara hi tha**: LinkedIn ka India-specific subdomain (`in.linkedin.com`) match hi nahi ho raha tha, sirf bare `linkedin.com` accept ho raha tha — fix kiya (`.linkedin.com` se end hone wala koi bhi subdomain ab accept), regression tests se confirm kiya purane cases abhi bhi sahi hai. Fir ek fresh lead ("Chahal Academy") pe poora positive case dikhaya — real Instagram+Facebook mile, real UI pe sahi dikhe. VPS pe fix deploy kiya.
  - **Follow-up: user ne pucha "Instagram/Facebook proper mil rahe hai ya issue hai"** — check kiya, honestly bataya: 604 real leads me se sirf 2 pe social set tha, aur wo dono maine manually test kiye the leads the — bug nahi, sirf ye ki `_handle_enrich` sirf DISCOVER ke time ek hi baar chalta hai, aur 604 me se zyadatar leads is feature ke bante hi se pehle enrich ho chuke the, to unpe kabhi chala hi nahi. **40 real leads pe real backfill chalaya** (sirf free website-footer path, credits use nahi kiye, jaisa user ne bola tha) — result: 37 ko try kiya, **20/37 (54%) me real social link mila** — genuinely achha free hit-rate, DB me persist ho gaya (instagram 2→20, facebook 2→21, linkedin bonus me 0→5).
  - **Isi bade sample se 4tha real bug mila**: "LK Academy Rajkot" ki apni website ke footer me LinkedIn ka ADMIN-panel link tha (`company/.../admin/updates`) — unke developer ne galti se apna internal "manage page" link public site pe daal diya tha, jo humare `company/` prefix check ko structurally pass kar gaya lekin click karne pe login-wall pe le jaata, kaam ka nahi. Real HTML check karke confirm kiya ye genuinely unki site pe hai. Dono jagah (website scraper + search fallback) fix kiya — "admin" segment wale koi bhi LinkedIn path ab reject hote hai. DB me pehle se save bad value clear ki, VPS pe deploy kiya.
- **User ne pucha "54% ko 80-90% tak kaise le jayein" — real investigation kiya, guess nahi.** 17 misses me se: 2 sirf transient the (dobara try karne pe mil gaye — imsindia.com, ekoching.com), 2 unki apni site ka permanent infra issue hai (broken SSL, timeout — humari taraf se fix nahi ho sakta), aur **sabse bada real bug**: kai businesses apna Instagram/Facebook link `href=` tag me nahi, JSON-LD structured data (`"sameAs":[...]`) ya embedded page-JSON me daalte hai (modern SEO practice) — humara regex sirf `href=` dekh raha tha, isliye miss ho raha tha. Fix kiya (ab koi bhi quoted URL match hota hai, JSON-escaped slashes bhi handle kiye) — isi fix se **54% se 75% tak jump hua** real 37-website test pe.
  - **Fix karte hi ek aur real bug mila**: kai jagah `facebook.com/tr` capture ho raha tha — ye Facebook ka apna tracking-pixel (Meta Ads) endpoint hai, real business page nahi. Precisely fix kiya (exact "tr" segment match, generic "tr" prefix se nahi, taki real handle jo "tr" se shuru hote hai jaise "trueblueacademy" wo galti se reject na ho). Dobara test kiya — **75% clean data ke sath confirm** hua. VPS pe deploy ho gaya.
  - **80-90% tak jaane ka asli rasta**: aaj jo bhi test kiya wo sirf FREE path tha (credits bachane ke liye) — real production pipeline (`_enrich_social`) already dono use karta hai: free website-scrape + paid Serper search fallback (jo already Sparrk/M.M. Shah Class pe kaam karke dikha chuka hai). Matlab real production hit-rate already 75% se zyada honi chahiye. Aur levers jo discuss kiye but abhi nahi banaye: automatic retry on transient failures (~12% free me recover hota hai), slow servers ke liye longer timeout, aur home/contact ke alawa `/about`/`/connect` jaisi pages bhi try karna.
- **User ne pucha: website hi nahi hai to Instagram/Facebook kaise laayenge (141/604 leads ki koi website nahi hai)** — bataya ye already bana hua hai (`find_social_profiles`, search-based), phir user ne redirect kiya: pehle noisy-query problem fix karo (jo `find_social_profiles` ko already affect kar rahi thi, TIME Ahmedabad wale case me). **Real data pe verify karke** (30 real addresses hand-check + poori 587-lead database pe coverage test) ek `_extract_city()` helper banaya jo scraped address se sirf city nikalta hai (86% database pe sahi kaam karta hai, Canadian leads correctly untouched rehte hai). TIME Ahmedabad ka case fix ho gaya (ab real Instagram+Facebook milta hai).
  - **5 functions me laga diya tha initially, lekin real regression test se ek genuine risk mila**: "MILESTONE ACADEMY" ke case me chhoti query ne Google ka snippet-excerpt badal diya, jisse business ka apna sahi Facebook page (jisme sahi email tha) us baar email dikha hi nahi paya snippet me, aur ek unrelated OCR'd-flyer post ka galat email jeet gaya. **Real side-by-side comparison karke confirm kiya** (purani vs nayi query, raw Serper results dekh ke) — guess nahi kiya.
  - **User ko clearly bataya trade-off, unhone decide kiya**: `find_website`/`find_social_profiles`/`find_review_signals` pe fix rakha (inke apne strict safety-checks hai), `find_phone`/`find_email` (jo REAL outreach ko feed karte hai) purani full-address query pe wapas revert kiya — kyunki galat business ko message jaana real mis-send hai, sirf missed lead se zyada risky. Revert ke baad phone/email wapas exact original correct values de rahe hai (verify kiya), social discovery ka fix abhi bhi kaam karta hai. VPS pe deploy ho gaya.
- **User ne "haan" bola to real credits use karke 15 no-website leads pe test kiya** — 53% (8/15) pehli baar. **Isi se ek aur bada real bug mila**: kuch business names Google Business pe decorative Unicode style me hote hai (jaise "𝗖𝗮𝗿𝗲𝗲𝗿 𝗙𝗶𝗿𝘀𝘁 𝗜𝗔𝗦 𝗔𝗰𝗮𝗱𝗲𝗺𝘆") — humara naam-matching regex sirf plain ASCII letters accept karta tha, to poora styled naam hi discard ho jaata tha, sirf baaki plain text bachta tha (galat "business name" signature ban jaata tha). Ye bug 3 jagah duplicate tha (`_name_matches_blob`, `_is_own_profile_link`, `_own_social_profile_field`) — matlab sirf social nahi, phone/email/website discovery ko bhi affect kar raha tha kisi bhi Unicode-styled-naam wale lead ke liye. Ek shared helper banaya (`_name_words`) jo Unicode normalize karta hai pehle (`𝗖𝗮𝗿𝗲𝗲𝗿` → `Career`), sab jagah use kiya. Real test kiya — dono failing leads ab real match dete hai, **hit-rate 53% se 67% ho gaya** isi ek fix se. VPS pe deploy ho gaya.
- **Sent/Seen/Replied chart — 3 real rejections in one sitting before landing on the right chart form (2026-08-18), worth remembering as a pattern.** None of the 3 rejections were about wrong DATA — the underlying per-day cohort logic (of what was sent on day X, how many of those SAME messages were later seen/replied, following forward in time) was correct from the first build. What kept being wrong was the VISUALIZATION FORM: (1) an all-time aggregate grouped-bar chart, rejected — user wanted a selectable single period, not one all-time number; (2) a 7-days-side-by-side multi-day bar chart, rejected — "alag alag lines nahi chahiye," wanted ONE selectable period (today/week/month/custom/all-time), not several days plotted together; (3) a single-period nested/bullet bar per channel, rejected — wanted a "proper graph ya pie chart," and their own explanation while rejecting it ("500 ko hi msg gaye... total 500 hi hai, 850 nahi") was itself the spec for the fix: the numbers needed to visibly sum to the real total, not just be legible. **Landed on**: one donut per channel, 3 mutually-exclusive buckets (Replied / Seen-no-reply / Not-seen) that sum EXACTLY to that channel's Sent count — genuinely valid part-to-whole, unlike a raw Sent/Seen/Replied pie which would be wrong (Seen and Replied are subsets of Sent, not additive slices; summing them fabricates a total, e.g. 500+250+100=850 when the real total sent is 500). Also actually re-validated the bucket color palette with the dataviz script for this specific new grouping (didn't just assume prior use made it safe) and, since no browser-automation tool exists in this environment, used the machine's own installed Chrome in headless mode against a standalone HTML harness to actually SEE the rendered chart before calling any of these three attempts done — caught a real "nested bars visually melt into each other" issue this way on attempt 3, before that attempt was itself superseded by the donut. **Lesson for next time a chart gets rejected: ask what's specifically wrong with the FORM, don't just assume the data or the colors are the problem** — this user's rejections were consistently about form/shape, and the data underneath was right every time.
- **Phase 3 DoD Gate re-verified explicitly before Phase 4 started** (user asked directly — see `feedback_verify_dod_gates_explicitly` in auto-memory) — 2 items had only ever been mock-tested (unsubscribe endpoint itself, QC rejecting a real bad draft); both real-tested for the first time and passed. Gate is now genuinely green, not just narratively claimed.
- **First live run surfaced two real incidents, both caught and fixed the same day:** (1) the autonomous outreach tick would have auto-emailed real discovered businesses with zero confirmation — user caught this ("kisiko outreach na ho jaye"), fixed with a kill-switch (now `system_settings.autonomous_outreach_enabled`, dashboard-toggleable, defaults OFF); (2) Gemini's 20/day free quota got exhausted mid-run, and although a provider-fallback fix was written, the already-running processes had the old code in memory (Python doesn't hot-reload) — 80 of 81 real scored leads got a fake "scoring failed" result during that window, caught via `agent_events` audit, fixed by restarting + re-enqueueing REVIEW for all 80.
- **Step 4.4 (React dashboard) built ahead of order**, at the user's request, once real leads had nowhere to be seen except by asking Claude to query the DB directly. Vite+React+Tailwind v4, Dashboard (Kanban pipeline + HOT-lead alerts) + Products (list + AI-strategy view + add form) pages. Also added `system_settings`-backed Discovery/Outreach ON-OFF toggles right on the dashboard (checked fresh from the DB every scheduler tick, no restart needed to take effect) after the user asked for exactly that. Known bug not yet fixed: claiming a HOT lead makes it vanish from the Kanban board entirely (`HOT_LEAD` isn't a displayed column).
- **Operational lesson:** discovery running live burns real Serper/LLM credits fast (30 searches + 544 LLM calls in under an hour before the user flagged it) — default posture now is discovery OFF unless a specific test needs it, using the new dashboard toggle instead of a process restart.
- **Demo-resend request found 3 more real bugs, all fixed same session:** (1) QC kept rejecting good drafts on multi-pain-point leads because its prompt read "reference the pain point" as needing ALL of them when the Outreach Agent is designed to pick just one — reworded to "at least one is enough." (2) A draft actually sent with a literal `"Best,\n[Your Name]"` signature — LLM instruction-following + QC's own judgment both missed it. Fixed with a deterministic regex stripper (`agents/outreach_agent.py`'s `_strip_signature()`) applied before QC even sees the draft, not just relying on the prompt. (3) OpenAI fallback model was stale (`gpt-4o-mini`) — queried the real API for available models rather than guessing, landed on `gpt-5.4-mini`. Re-verified the full flow clean afterward: real email + real WhatsApp, no defects, lead `OUTREACHED`.
- **Real wrong-email bug, user-spotted from a dashboard screenshot:** 9 unrelated real businesses (all Instagram-only, no real website) had all somehow gotten the exact same email, `it@instagram.com`. Root cause: `extract_domain()` took Instagram's own domain off the profile URL and treated it as the business's company domain, so the enrichment waterfall scraped Instagram's OWN real pages and picked up Instagram's own generic contact address -- `belongs_to_company()`'s domain-match check was working correctly, the domain handed to it was just wrong. Fixed: `extract_domain()` now returns `None` for known social-profile hosts (reused `serp_provider.py`'s existing `SOCIAL_PROFILE_HOSTS`, renamed from `_PROFILE_HOSTS` rather than duplicating the list). Cleared the 9 corrupted emails in the real DB. Phone numbers on the same leads checked and confirmed NOT affected (different data path).
- **Root-cause fix, real bug:** email kept getting QC-rejected (WhatsApp never did) because `quality_controller_agent.py`'s `review_draft()` was never actually given the product brief -- QC had no ground truth to check capability claims against, so it treated every real, product-description-backed claim ("we build custom apps...") as an invented one and rejected it. Fixed by threading `product_brief` through to QC (prompt updated to judge claims against it, not against nothing). Verified real: same demo lead, both channels sent clean on the first attempt afterward.
- **WhatsApp inbound proven fully real (2026-08-14), not just simulated.** Set up ngrok (official binary -- npm's `ngrok` package didn't yield a runnable CLI; Windows Defender flagged the exe as a false positive, user allowed it manually) to get a real public URL, verified it actually reached the local Flask app before touching anything else. Found a real, undocumented BSP config bug by elimination: a "Wrapper Client" toggle defaulted to "Panel Mode," which silently swallowed inbound messages (real double-tick delivery confirmed on WhatsApp, but zero requests ever reached our webhook, verified via ngrok's own request log) -- switching it to "Wrapper API Only" fixed it immediately, real message arrived and matched to the right lead within seconds. Both inbound channels (email + WhatsApp) are now verified with genuinely organic real messages, not synthetic test payloads.
- **MAJOR real bug fixed (2026-08-14): the standalone `python -m jobs.worker` background process has never actually processed anything, since Step 2.1.** Classic Python gotcha: running a file with `-m` loads it as `__main__`, a SEPARATE module identity from its package-qualified name -- so every handler module's `from jobs.worker import register_handler` imported a SECOND, empty-`HANDLERS` copy of worker.py, and every registration landed in a dict the actual running `__main__` instance never looked at. Every real send this whole project has made worked only because a one-off test script imported the handler in the same process before calling `run_once()` -- the long-running background process itself was always inert. **Fixed** by splitting the registry into its own `jobs/registry.py` (never run as `__main__`, so never hits this bug) that both `worker.py` and every handler now import from. Verified for real: restarted the actual background process, it picked up and correctly processed a pending job with zero manual help. **Lesson: a background process "appearing to work" because manual test scripts kept covering for it is not the same as it actually working -- worth periodically verifying the standalone/production entrypoint itself, not just the logic it wraps.**
- **Step 4.3 — AI intent classifier + escalation guardrail (2026-08-14).** First (and only) LLM call in the inbound path, `agents/inbound_agent.py`'s `classify_intent()` — classifies replies into INTERESTED/DEMO_REQUESTED/OBJECTION/STOP/AUTO_REPLY and always drafts a `suggested_reply`. INTERESTED/DEMO_REQUESTED and anything pricing/legal/hostile-sounding **always** escalate to a human regardless of AI confidence — the highest-value moments in the pipeline never get left to auto-reply. Only a low-risk, high-confidence OBJECTION can ever be auto-sent, and only if the new `system_settings.auto_reply_enabled` dashboard switch is on (default OFF, same posture as the other kill-switches) AND the reply passes the same QC veto fresh outreach drafts go through. Verified real end-to-end with a genuine sent email on the GameZone Visnagar lead.
  - **New capability (2026-08-17): acknowledgment reply.** User-identified real gap: an inbound message escalated to a human (INTERESTED/DEMO/etc.) got zero reply until a human acted -- bad engagement at the highest-value moment. Built a short, safe, personalized "we got your message, we'll follow up shortly" holding reply, sent immediately, on top of (not instead of) normal escalation. New `acknowledgment_reply` field drafted by the same classifier LLM call; new dashboard switch `acknowledgment_reply_enabled` (default off); new `whatsapp_service.send_free_form_message()` (first non-template WhatsApp send in the project, legal here because Meta's 24h window is always open when replying to a just-arrived message). **Real bug found live same day**: the first attempts got rejected by the existing pitch-reviewing QC prompt for "not referencing a pain point" / "vague" -- but vague-and-brief is the acknowledgment's entire point. Fixed with a separate, lighter `review_acknowledgment()` QC pass (`ACKNOWLEDGMENT_QC_SYSTEM_PROMPT`) that only checks for buzzwords/capability-claims/footer, not specificity. Verified real end-to-end on both channels. Also fixed in passing: `auto_reply_enabled` had a backend but was never actually wired into the dashboard UI -- added it alongside the new switch.
  - **Real bug + a design-system fix, same day, from user feedback on the funnel chart.** Small nonzero bar values (1 lead) rendered as a circle/blob, not a short bar -- `rounded-full` on BOTH ends of a bar whose minimum-width percentage was narrower than its own height made short segments read as dots regardless of corner style; fixed to `rounded-r-[4px]` (only the data-end rounds, matching the dataviz skill's actual mark spec) plus a taller effective minimum width so short bars stay wider than they are tall. Separately, at the user's request: every lead STATUS now has one fixed color used everywhere it appears (`frontend/src/lib/statusColors.js`, new shared source of truth) -- Kanban column headers, LeadDetail's status badge, and the Analytics funnel all read from the same map, instead of the funnel using a generic blue ramp and the status badge always being plain gray.
  - **CRM upgrade Phase 3 — Analytics / Charts (2026-08-17).** New `/analytics` page: pipeline funnel (honest cumulative "reached at least this stage" count -- the schema has no stage-transition history, so this deliberately isn't a fabricated conversion-history metric), channel performance (Email vs WhatsApp, real reply rates), trend line chart, per-product table. Followed the `dataviz` skill throughout, including actually running its palette validator script (not eyeballing) -- it flagged the aqua/replies series below 3:1 contrast on the light surface, mitigated with a "View as table" toggle per the skill's own relief rule. All 4 backend endpoints verified against real data before the frontend was built.
  - **Real bug, same page, same day: `type="password"` on a secret-entry field invited browser autofill to silently inject an unrelated saved credential** (the autofill doesn't fire a normal input event, so what's visibly shown stops matching React's real state -- copy/paste on it does nothing). Fixed by using `type="text"` + `autoComplete="off"` + `data-lpignore`/`data-1p-ignore` instead -- there was no real masking need anyway since the field is always empty by default (secrets are write-only, never pre-filled).
  - **CRM upgrade Phase 2b — dedicated Settings page covering all of `.env` (2026-08-17).** User wanted every remaining `.env` value (including real API keys) visible/editable from the dashboard. Confirmed the security approach explicitly before building: secrets are write-only and masked -- the real value never gets sent back to the browser, only "configured + last 4 chars" status. New `services/env_settings.py` (whitelist registry, safe in-place `.env` file read/write, never an arbitrary key write) + `/settings` page (moved `SystemToggles` here from Dashboard, added hint tooltips per field, secrets as masked password inputs). Explicitly surfaces that `.env` changes need a process restart to take effect (unlike the hot `system_settings` values) -- both in the API response and the UI, not left implicit. Verified live: real 50-line `.env` file unchanged in line count after a real write, target line replaced in place, no corruption.
  - **CRM upgrade Phase 2 — Settings expansion (2026-08-17).** Moved EOD report recipients (email + WhatsApp), daily send caps, and discovery cooldown hours from `.env`-only `Config` into dashboard-editable `system_settings` (same no-restart-needed pattern the boolean switches already had) -- `Config`'s value is now just the fallback default. Found and fixed, in passing, the same India-hardcoded-WhatsApp bug class as the international-phone fix: the EOD report's own WhatsApp send also had a bare `f"91{phone}"`, now goes through the real `normalize_phone()`. Verified live via real GET/PATCH round-trips including a wrong-type rejection test.
  - **CRM upgrade Phase 1 + a real international-phone safety bug (2026-08-17).** Built the first CRM upgrade phase (`CRM_UI_UX_PLAN.md`): a full Lead Detail page (`/leads/:id`) with real timeline (merged from agent_events/outreach_logs/inbound_conversations), a WhatsApp-style Conversation tab (chat bubbles, auto-scrolls to newest), editable contact info, score breakdown. Design iterated live off the user's own screenshots (read/edit toggle, side-by-side layout on wide screens, etc.) -- self-caught one real bug in the process (deriving a Tailwind class name at runtime via string replace silently produces no CSS, same lesson as the earlier toggle-knob bug: never construct Tailwind classes dynamically). **Bigger find**: while looking at a real Canadian lead's number on this new page, the user asked if the system could actually WhatsApp it -- investigation found `normalize_mobile()` was hardcoded India-only everywhere a WhatsApp number gets formatted for sending, and a real Edmonton lead's toll-free number coincidentally matched the Indian-mobile shape, meaning a real send would have gone to a fabricated, unrelated number, not the lead. Fixed properly with the `phonenumbers` library + a new `Product.target_country` field (not a quick patch) -- verified live against all 10 real Edmonton leads' actual numbers, zero regression confirmed for India. Never triggered an actual real send to test it (autonomous outreach stays off; unauthorized real sends to third parties are never acceptable even for testing). **Known, deliberately deferred follow-up**: inbound WhatsApp webhook matching is still India-only, so a real Canadian reply would currently be silently dropped -- documented in tracker.md, not yet fixed (lower urgency, no Canadian lead has messaged in yet).
  - **Step 4.5 — EOD executive report (2026-08-17), Phase 4's last remaining piece -- DoD Gate P4 now green, Phase 4 fully complete.** `services/reporting_service.py` aggregates real metrics for the actual IST calendar day (leads discovered, scored by tier, outreach sent by channel, replies, high-intent replies, human escalations across every agent) straight from existing tables -- no new tracking infra needed. A narrowly-scoped LLM prompt (`EOD_SUMMARY_SYSTEM_PROMPT`, deliberately NOT the full Phase-5 CEO agent with targets/campaign_actions) writes a grounded <=120-word summary, forbidden from inventing numbers. KPI honesty: `spam_rate`/`intent_classification_accuracy` stay permanently `null` since no real signal exists to compute them from yet -- never fabricated. New `send_internal_email()` (no marketing footer, this is an internal report not lead outreach). Scheduled as a third tick inside the already-running `jobs/discovery_scheduler.py` (once daily after 23:50 IST, idempotent per date) plus a manual `POST /api/v1/reports/generate` for on-demand testing. Verified live: real email delivered to `ivaiagent05@gmail.com` with real counts, confirmed independently via Resend's own API; re-triggered immediately after with no duplicate send (idempotency confirmed). Same day, extended to also send via real WhatsApp (`send_free_form_message()`) to the user's number -- both email/WhatsApp recipient lists are env-configurable for now (`EOD_REPORT_RECIPIENTS`/`EOD_REPORT_WHATSAPP_RECIPIENTS`), user explicitly wants these dashboard-editable eventually, not built yet. Verified live: both channels confirmed actually sent.
  - **Discovery-starvation bug + per-product toggle (2026-08-17):** onboarding a new real product (Barber shop management software, Edmonton) surfaced a real scheduler bug -- older products with a large enough (query x region) combo space fill the whole per-tick budget every time, so the scheduler's product loop (fixed insertion order, no rotation) never even reaches newer products; a brand new product could get starved indefinitely. First fix attempt (order by `Product.updated_at` ascending) was itself backwards -- a new product's `updated_at` IS its most recent timestamp, so that ordering pushed it to the back, not the front. Real fix: order by each product's MAX `DiscoveryRun.last_run_at` (a product with zero runs ever has a NULL aggregate, which sorts first) -- correctly treats a never-discovered product as maximally overdue. Also added, at the user's request: a per-product discovery on/off toggle on the dashboard (the backend field `Product.is_active` already fully supported this end-to-end, including already being what the scheduler filters on -- pure frontend wiring gap). Verified fully live: with only the new product's toggle on, discovery correctly found 10 real Edmonton barbershops with real addresses.
  - **Redesign, same day: escalation reply replaces the minimal acknowledgment.** User's real pushback on the acknowledgment feature: the system already knows this lead's verified pain points AND the specific product/service it was approached about (every lead is approached about exactly one product) -- so a genuinely helpful, grounded answer is possible with zero hallucination risk, not just "we got your message." Also required: a reply must ALWAYS go out, never silently give up. Retired the separate minimal `acknowledgment_reply`/its own lightweight QC prompt entirely -- `suggested_reply` now does this job directly (adaptively answers using only verified pain points + product_brief, redirects pricing/exact-demo-time to the human, always closes with "team will follow up shortly"), reviewed by the SAME real `review_draft()` QC as everything else. New: if QC rejects, the AI gets one/two chances to fix the SPECIFIC issues QC raised (`redraft_reply()`); if still rejected, a fixed, non-AI fallback message sends instead -- guarantees a reply always goes out. **Real bug found live immediately**: QC's own existing "no fabricated timeline" / "no self-added footer" rules (written for cold outreach, where the system appends its own footer) correctly caught the new mandatory "team will follow up shortly" closing as exactly what they're designed to catch -- a genuine conflict between an old rule and a new requirement, not a bug in either alone. Fixed with an explicit carve-out in the QC prompt for that specific, always-true closing line. Verified live: the exact wording that failed 3 straight QC attempts was approved after the fix.
  - **Second duplicate-send bug (2026-08-17, Monday)**, found because the Friday fix above was never actually re-verified before the session ended. On a fresh full-system restart, re-testing the exact same race showed the 409 guard correctly blocking the racing second request -- but BOTH channels still sent twice anyway. Root cause: `claim_lead_for_outreach()` (services/lead_service.py) always enqueues `OUTREACH_EMAIL`/`OUTREACH_WA` jobs as part of its atomic claim (needed for the autonomous scheduler path) -- but the manual "Send Outreach Now" endpoint ALSO processes those channels synchronously right after claiming, so with `jobs.worker` actually running continuously (true now that the earlier registry bug is fixed), the same queued job gets picked up and sent AGAIN independently -- guaranteed, not just a race. Fixed with an `enqueue_jobs=False` option the manual endpoint now passes. Verified live: exactly one EMAIL + one WHATSAPP per click afterward, race-guard still correct. **Lesson reinforced hard by this one: a fix made late in a session is not trustworthy until re-verified after a real restart** -- see [[feedback_verify_dod_gates_explicitly]].
  - **Real duplicate-send bug (2026-08-14), found via the user's own normal dashboard use, not reckless clicking:** clicking "Send Outreach Now" once produced 4 duplicate real EMAIL + 4 duplicate WHATSAPP sends within ~21 seconds. Root cause was two bugs compounding: (1) `PipelineKanban.jsx` renders each status column via its own separate `.map()` -- when the lead's status flips to `OUTREACHING` (happens immediately, before the slow LLM-draft+send work finishes), the next dashboard poll moves it to a different column's array, which REMOUNTS its `LeadCard` with fresh `sending=false` state (a React `key` only dedupes within the same parent array, not across different `.map()` calls) -- so the button looked freshly clickable mid-send, not because the user clicked recklessly but because the UI itself was lying. (2) `api/leads.py`'s `trigger_outreach()` unconditionally force-reset `lead.status` back to `SCORED` on every call (meant to let a demo always re-trigger), which defeated `services/lead_service.py`'s `claim_lead_for_outreach()`'s own atomic `WHERE status='SCORED'` guard -- a second call arriving mid-flight just got reset and re-claimed instead of being blocked. Fixed both: backend now returns 409 if a send is already `OUTREACHING` instead of resetting it; frontend's button-disabled check now also reads `lead.status==="OUTREACHING"` from server props (remount-safe) instead of trusting only local component state. Also fixed the known Kanban gap that motivated this test in the first place: `HOT_LEAD` was excluded from displayed columns as a "rare/terminal" assumption predating Step 4.3's inbound escalation, which made it common -- added a "Hot / Escalated" column.
  - **Real cross-channel live test (2026-08-14):** user sent real replies on both email and WhatsApp to the same real outreach, to see how the system handles a business replying on multiple channels. Surfaced 3 more real bugs, all fixed same session: (1) `record_inbound()`'s row-insert and job-enqueue were two separate commits — if the second ever failed, a message stayed recorded forever with no classification job and no retry path (fixed: folded into one atomic commit). (2) **Severe**: `is_optout()` keyword-matched the whole message body including quoted content — since every outbound email has an "Unsubscribe" footer and most email clients quote the original on reply, ANY genuine reply that kept the quote got misread as an opt-out and really suppressed (fixed: strip quoted/original content before any hard-classifier keyword match, `_strip_quoted_reply()`). (3) `jobs.worker` and `jobs.inbound_poller` were both running stale in-memory code from before today's fixes (classic no-hot-reload issue, same shape as the earlier `jobs.worker` registry bug) — the false-STOP bug kept refiring every poll cycle until the poller was actually restarted; `app.py` turned out fine since Flask's debug reloader auto-restarts on file save. Full detail in tracker.md Step 4.3. **Still open**: per-channel-only suppression scoping (a STOP on one channel doesn't suppress the lead's other channels) — identified, not yet fixed.
  - **Real bug caught by the user re-reading the actual sent reply, same day:** the auto-reply mentioned "queue management" — never one of GameZone's actual verified pain points (only `NO_ONLINE_BOOKING` and `MANUAL_BILLING_ERRORS` were on file). Root cause: `classify_intent()` was only ever given the generic product brief, never the lead's own verified `pain_points_extracted` — so it had no ground truth and invented a plausible-sounding but unverified detail. Same gap on the QC side (`review_draft()` was called with `pain_points=[]`). This is the same bug shape as the earlier "QC never got the product brief" fix, just on the inbound side. **Fixed**: both `classify_intent()` and the inbound QC call now receive the lead's real `LeadReviewInsight.pain_points_extracted` (same source `outreach_handler.py` already used). Re-verified live with the real LLM (OpenAI fallback) — new reply correctly referenced only the real verified pain points, no invented framing.
- **Step 4.2 — Hard pre-classifiers (2026-08-14).** Rule-based (no LLM at all -- there's no LLM in the inbound path yet) STOP/opt-out and auto-reply detection, `cognition/hard_classifiers.py`. STOP detection deliberately includes "not interested" per MASTER's exact spec, not just literal STOP -- reuses the existing `suppression.py` (same single source of truth as the outbound side). Auto-reply detection prefers real email headers (`Auto-Submitted` etc.) over guessing from body text when available; WhatsApp has no headers so it's body-text-only there. Wired into `record_inbound()` so both channels get it automatically. 30 checks total (18 unit + 12 real DB integration), including proving a header-only auto-reply (neutral body text) still gets caught, and that a genuine reply correctly leaves `intent_detected` NULL for Step 4.3 to fill in later.
- **Step 4.1 — Inbound webhook + idempotency (2026-08-14).** Key design call: email replies don't come through Resend (Resend is outbound-only) -- they land via normal SMTP/MX at Hostinger, so email inbound uses **IMAP polling** (`jobs/inbound_poller.py`, no public URL needed) instead of a webhook; WhatsApp keeps a real Meta-style webhook (`api/inbound.py`). Both funnel through `services/inbound_service.py`'s `record_inbound()` (idempotent, matches sender to an existing lead). Scoped to receive+dedup+store only -- STOP detection (4.2) and AI classification (4.3) are next, not built yet. Hostinger mailbox `ronakpatel@ivinfotech.com` created live with the user; standard IMAP settings (`imap.hostinger.com:993`) worked first try. **Verified fully real**: WhatsApp tested with a simulated-but-real-shaped Meta payload (idempotency confirmed too); email tested by having the user send a genuine reply to an earlier real demo outreach email -- the poller captured and correctly matched it to the GameZone Visnagar lead, organically, not synthetically.
- **Branding + real per-lead outreach button:** user supplied a real logo (project root `logo.png`, slate/gray "A" mark) — wired into favicon + nav, whole frontend repainted from generic gray to a matching slate palette. Found and fixed a real toggle-switch bug (looked "on" when actually off — Tailwind translate-class ambiguity, replaced with explicit inline styles + ON/OFF text label). Built `POST /api/v1/leads/<id>/outreach` — a real, synchronous, human-triggered "Send Outreach Now" button per lead (for demos/manual resends), deliberately NOT gated by the autonomous-outreach kill-switch since it's an explicit single-lead human action, not the scheduler's bulk autonomous claiming.

**Local dev setup:** Python venv at `backend/venv` (no dot — differs from an earlier assumption; not committed, gitignored). Real `.env` exists locally (gitignored) with Gemini/OpenAI/Serper/Hunter keys.

### Key lessons from Phase 2 (the reusable wisdom — full bug-by-bug narrative is in tracker.md §2 if ever needed)

1. **A paid enrichment API's silence is not evidence a contact doesn't exist.** Hunter can only return `@company-domain` emails; it structurally cannot see the gmail/yahoo addresses most Indian SMBs actually use. Always ask "what can this provider structurally never return?" before trusting an empty result.
2. **The single most common accuracy bug this project has hit: loose name-matching lets one business's data attach to a different lead.** Happened independently in `find_phone`, `find_website`, and `find_email` before a shared, reused fix (`_name_matches_blob()`, plus `_is_own_profile_link()` for URL-handle trust) closed all three. **Rule going forward: any new "does this result belong to this business" check must reuse these shared helpers, never a fresh bespoke check — bespoke matching has been wrong every single time it's been tried here.**
3. **Two real businesses can share a name across different cities** (e.g. "Infinity Gaming Zone" Ahmedabad vs "Infinity Gaming" Navsari) — name-matching alone cannot disambiguate this; needs real location/gazetteer logic. Known, deliberately unfixed limitation (see tracker.md §3 "Known open items").
4. **LLM model names go stale.** `gemini-2.5-flash` (MASTER PRD's named model) returned 404 for new accounts mid-project — use self-updating aliases like `gemini-flash-latest` where available.
5. **The swappable `LLM_PROVIDER` design (§4.1 below) paid for itself the first time it was needed** — when Gemini's free-tier daily quota (20 requests/day) was exhausted mid-testing, switching to OpenAI for the rest of that session's verification was a one-env-var change, not a rewrite.
6. **Test against independently-known ground truth, not just "the code runs."** Nearly every real bug above was caught by checking a result against something already verified another way (a user's own screenshot, a Google Maps read, a site's own `<title>` tag) — a green test suite alone missed all of them initially.

## 4.1 Architectural deviation — LLM provider (2026-08-11, full detail in tracker.md §A.1)
MASTER PRD hardcodes Gemini 2.5 Flash; user confirmed instead a **single swappable provider** (`config.py`: `LLM_PROVIDER` + `LLM_MODEL`, `cognition/llm_client.py` branches on it) — provider swap = one config change. `LLM_PROVIDER=gemini` is the default (genuinely free tier); `openai` is the tested fallback, already proven useful once when Gemini's daily quota ran out mid-session.
**Update 2026-08-19:** local aur VPS ab deliberately alag hain — **VPS par `LLM_PROVIDER=openai`** (Gemini ke baar-baar 429/503 free-tier failures live outreach ko rok rahe the), **local par `gemini`** waisa hi. Fallback dono jagah dono taraf kaam karta hai. Ye bilkul wahi swappability hai jiske liye ye design banaya tha — ek `.env` line, koi code change nahi.

## 4.2 Add-on Phases 6–10 planned (2026-08-19) — post-launch requirements
System live hone ke baad user ne 11 naye requirements diye (`NEW_REQUIREMENTS_STAGING.md` me raw capture, ab sab `MERGED`). Inhe **aur har wo point jo pehle hold par tha** (Phase 5 ke alawa) ek proper phase plan me convert kiya, teeno docs me: `MASTER_DEVELOPMENT_PRD.md` §5A (Phase 6–10 + P6–P10 gates), `AI_Sales_Intelligence_PRD_v2.md` Chapter 16 (cognitive contract), `CRM_UI_UX_PLAN.md` §2A (UI Phase 5–9, backend ke saath 1:1). Poora breakdown tracker.md **Section 5** me.

- **Phase 6** Live observability → **7** Targeting + person-level contacts → **8** Message format engine → **9** Measurement + multi-touch + adaptive templates → **10** Channel expansion (SMS/social/voice).
- **Sequencing ka asli reason (yaad rakhna, ye preference nahi hai):** ⭐ WhatsApp-template-loop 2026-08-13 ko sirf isliye defer hua tha ki *real performance data nahi tha (`campaign_variants` wired nahi)*. Wahi gap Item 6 (subject-line testing) aur Item 4d (AI templates) ko bhi block karta hai. Isliye **variants banao (P8) → measure karo (P9 early) → tabhi AI adapt kare (P9 late)**. Ulta karne se ek aisa "learning" agent banega jo actually guess kar raha hoga — bilkul wahi cheez jisse bachne ke liye defer kiya tha.
- **Hold-par-rakhe points jo fold ho gaye:** ⭐ template loop (→ P9.6), Hunter ke discard ho rahe person fields (→ P7.4, zero naya cost), `campaign_variants` wiring (→ P9.1), 604-lead social backfill (→ P7.7), teen discovery bugs — city filter/cross-city collision/multi-branch (→ P7.6), §12 ka Voice SDR Agent (→ P10.4).
- **✅ DECIDED (2026-08-19): Phase 5 indefinitely postponed, sequence = 6 → 7 → 8 → 9 → 10.** User ka call. Phases 6–10 ki Phase 5 par koi technical dependency nahi hai, aur Phase 5 ke saare modules (CAC ceiling, capacity throttle, renewal lifecycle) volume-dependent hain — converted clients aur delivery-capacity pressure abhi hai hi nahi, to wo ek non-existent problem solve karta. **Phase 5 ka spec delete nahi kiya** — MASTER §5 me intact hai, P5 gate bhi §9 table me hai; jab real capacity pressure aayegi tab cleanly slot ho jayega. Formal deviation record: tracker.md **§A.6**. **Jo nahi badla: har naye phase ka DoD gate (P6–P10) utna hi mandatory hai** — phase order chhodna gates chhodna nahi hai.
- **Do requests ka jawab "nahi ban sakta" hai, aur wo docs me honestly likha hai:** LinkedIn cold-messaging ka koi official API hai hi nahi; Meta (IG/FB) sirf reply-window me messaging deta hai, cold DM ka koi template-rasta nahi hai. Bot se bhejna = ToS violation + permanent ban risk + project ke apne evasion-free rule ke against. Isliye **draft-and-queue** design: AI sab kare except send, human bheje. P10 ka gate isko *absence se* verify karta hai — aisa koi code path hona hi nahi chahiye.

## 5. Important technical rules (full list `tracker.md` Section 1 me hai)

Quick highlights — poori list ke liye `tracker.md` dekho: secrets sirf `.env` me kabhi source me nahi, SQLite pragmas per-connection zaroori, OPT_OUT/suppression 100% rule ahead of AI processing, QC veto absolute, HUMAN_LOCKED_PARAMS (pricing/discounts/SLA) kabhi autonomous nahi, atomic claims (rowcount check) har concurrency-sensitive jagah, WhatsApp sirf official Cloud API.

---

**Bottom line for a fresh session:** Naya kaam start karne se pehle — ye file padho, phir `tracker.md` ka Section 3 (Ongoing) aur Section 4 (Pending) dekho ki abhi kahan tak kaam hua hai, phir wahi collaboration protocol follow karo (explain → confirm → build → tracker update).
